name: Linux

on:
  pull_request:
  push:
  release:
    types: published

jobs:
  build:
    runs-on: [ ubuntu-latest ]
    container:
      image: centos:7
    steps:
      - uses: actions/checkout@v2
      
      - name: Cache compiler
        if: ${{ !env.ACT }}
        id: prepare-compilers
        uses: actions/cache@v2
        with:
          path: ~/
          key: ${{ runner.os }}-${{ hashFiles('native/centos7-setup-llvm.sh') }}

      - name: Check build
        shell: bash
        run: pwd && ls -lah

      - name: Prepare build
        shell: bash
        run: cd; source polyregion/native/centos7-setup-llvm.sh setup-build || true

      - name: Prepare compilers
        if: steps.prepare-compilers.outputs.cache-hit != 'true'
        shell: bash
        run: cd; source polyregion/native/centos7-setup-llvm.sh setup-llvm || true

      - name: Configure release build
        shell: bash
        working-directory: ./native
        run: ./build.sh configure .

      - name: Release build shim
        shell: bash
        working-directory: ./native
        run: ./build.sh build polyregion-shim-jvm

      - name: Release build runtime
        shell: bash
        working-directory: ./native
        run: ./build.sh build polyregion-runtime-jvm

      - name: Release build compiler
        shell: bash
        working-directory: ./native
        run: ./build.sh build polyregion-compiler-jvm

      - name: Check artefact
        shell: bash
        working-directory: ./native
        run: file build-linux-x86_64/bindings/jvm/*
