name: Linux x86_64 build

on:
  pull_request:
  push:
  release:
    types: published

jobs:
  release:
    uses: ./.github/workflows/linux-shared.yaml
    secrets: inherit
    with:
      build_arch: x86_64
      build_type: Release
