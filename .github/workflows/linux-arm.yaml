name: Linux arm build

on:
  pull_request:
  push:
  release:
    types: published

jobs:
  release:
    uses: ./.github/workflows/linux-shared.yaml
    secrets: inherit
    with:
      build_arch: arm
      build_type: Release
